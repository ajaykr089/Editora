---
title: Toggle
description: Interactive pressed-state control for toolbars and compact action groups.
sidebar_label: Toggle
---

# Toggle

```tsx
import { Toggle } from '@editora/ui-react';

<Toggle variant="minimal" tone="brand" pressed>
  Pin
</Toggle>
```

## Supported Props

`pressed`, `disabled`, `loading`, `headless`, `size`, `variant`, `tone`, `shape`, `elevation`, `name`, `value`, `required`, `iconOn`, `iconOff`, `onInput`, `onChange`
