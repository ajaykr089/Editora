---
title: Switch
description: Switch wrapper for on/off settings with labels, descriptions, and event details.
sidebar_label: Switch
---

# Switch

```tsx
import { Switch } from '@editora/ui-react';

<Switch label="Enable alerts" description="Notify me when approvals are overdue." onChange={(detail) => console.log(detail.checked)} />
```

## Props

`checked`, `disabled`, `headless`, `loading`, `size`, `variant`, `tone`, `shape`, `elevation`, `label`, `description`, `name`, `value`, `required`, `onInput`, `onChange`
