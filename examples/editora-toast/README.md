# Editora Toast Example

Standalone Vite example for `@editora/toast`, designed to run independently in CodeSandbox.

```bash
npm install
npm run dev
```

Open `http://localhost:4174`.

The example covers stacked toasts, rich colors, title/description content, action/cancel buttons, promise toasts, progress updates, duplicate prevention, and the headless `subscribe()` API.
