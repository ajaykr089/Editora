import { defineConfig } from 'vite';

// This package is documentation-only (Angular integration guidance).
// Provide a no-op Vite config so tooling doesn't attempt to import non-JS files.
export default defineConfig({});
